<h1><%= t('views.common.title_show', name: Report.model_name.human) %></h1>

<p>
  <strong><%= Report.human_attribute_name(:title) %></strong>
  <%= @report.title %>
</p>

<p>
  <strong><%= Report.human_attribute_name(:description) %></strong>
  <%= @report.description %>
</p>

<% if @report.user == current_user %>
    <%= link_to t('views.common.edit'), edit_report_path(@report) %> |

<% end %>
<%= link_to t('views.common.back'), reports_path %>

<h2><%= Comment.model_name.human %></h2>

<% if @report.comments.present? %>
  <% @report.comments.each do |comment| %>
    <div class="comment">
      <p><%= comment.content %></p>
      <p><%= about_comment_user(comment) %></p>
      <p><%= commented_time(comment) %></p>

      <% if comment.user_id == current_user.id %>
        <% next if comment.new_record? %>

        <%= link_to t('views.common.edit'), edit_comment_path(comment) %> |
        <%= link_to t('views.common.destroy'), comment_path(comment), method: :delete, data: { confirm: t('views.common.delete_confirm') } %>
      <% end %>
      <p>-----------------------</p>
    </div>
  <% end %>
<% else %>
  <p><%= t('views.common.no_yet', name: Comment.model_name.human) %></p>
<% end %>

<%= render 'comments/form', commentable: @report, comment: @comments %>
