<p>
  <strong>Content:</strong>
  <%= @comment.content %>
</p>

<p>
  <strong>Report:</strong>
  <%= @comment.report_id %>
</p>

<%= link_to 'Edit', edit_comment_path(@comment) %> |
<%= link_to 'Delete', comment_path(@comment), method: :delete, data: { confirm: 'Are you sure?' } %> |
<%= link_to 'Back', report_path(@comment) %>
